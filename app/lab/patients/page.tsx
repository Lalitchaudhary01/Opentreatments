import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabPatientsPage() {
  return (
    <LabRoutePlaceholder
      title="Patients"
      description="View patients, test history and order lifecycle in one place."
    />
  );
}

import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabCollectionPage() {
  return (
    <LabRoutePlaceholder
      title="Sample Collection"
      description="Manage home collection pickups, slot assignment and collection queue."
    />
  );
}

import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabBookingsPage() {
  return (
    <LabRoutePlaceholder
      title="Bookings"
      description="Track booking lifecycle, assign technicians and monitor sample status."
    />
  );
}

import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabSettingsPage() {
  return (
    <LabRoutePlaceholder
      title="Settings"
      description="Configure notifications, process defaults and operational preferences."
    />
  );
}

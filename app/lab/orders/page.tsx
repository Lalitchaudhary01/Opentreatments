import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabOrdersPage() {
  return (
    <LabRoutePlaceholder
      title="All Orders"
      description="Track all incoming test orders, current status and pending actions."
    />
  );
}

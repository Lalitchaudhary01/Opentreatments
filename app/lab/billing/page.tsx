import { LabRoutePlaceholder } from "@/features/panel/lab";

export default function LabBillingPage() {
  return (
    <LabRoutePlaceholder
      title="Billing & Payments"
      description="Handle invoices, payments, settlements and reconciliation."
    />
  );
}
